# Mapbox GL JS access token (free tier is plenty for this app).
# Create one at https://account.mapbox.com/access-tokens/ and copy it here:
#   cp .env.example .env.local   then paste your token below.
VITE_MAPBOX_TOKEN=
